# Task
task: CA
mode: train
difficulty: easy

# Solver
solver: gurobi
max_time: 100
max_solutions: 500

# Parallel
workers: 8
threads: 1

# Paths
data_dir: ./data/${mode}/${task}/${difficulty}
output_dir: ./dataset/${mode}/${solver}/${task}/${difficulty}/${max_time}s/
